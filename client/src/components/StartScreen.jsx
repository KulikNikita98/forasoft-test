/**
 * StartScreen — стартовый экран (ввод имени, создание комнаты).
 * Полная реализация — задача 9.
 */
function StartScreen() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-gray-900 text-white">
      <h1 className="text-2xl font-semibold">Видеочат-комната</h1>
    </div>
  );
}

export default StartScreen;
